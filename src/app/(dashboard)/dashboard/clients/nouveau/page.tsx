import { ClientForm } from '@/components/clients/client-form'

export const metadata = { title: 'Nouveau client — Sous le Pommier' }

export default function NouveauClientPage() {
  return <ClientForm />
}
